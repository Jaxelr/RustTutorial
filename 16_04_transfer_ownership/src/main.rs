use std::marker;

fn main() {
    //No code, just information about the traits sync and send that are implemented
    //by the language.
    //The way to implement concurrency is using the safe mechanism provided by the standard library
    //such as: Mutex<T>, Arc<T>, RefCell<T>, Rc<T>, Arc<T>, etc... 
}
